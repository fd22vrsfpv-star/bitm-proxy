fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.5
python-multipart>=0.0.6
playwright>=1.40
cryptography>=42.0
pyyaml>=6.0
msgpack>=1.0
# TLS-fingerprint impersonation for the :3128 MITM upstream leg.
# Bundled libcurl-impersonate ships inside the wheels; no native build.
curl_cffi>=0.6
pywin32>=306; sys_platform == "win32"

# Phantom Join (Azure AD CA-bypass toolkit) — gated by the
# allow_phantom_join config flag (OFF by default). See
# tools/README_Phantom.md. ROADtools brings requests transitively.
roadlib>=0.25.0
roadtx>=1.10.0
roadrecon>=1.6.0
